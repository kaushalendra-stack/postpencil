import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Admin Settings',
  description: 'Platform settings and configuration for PostPencil administrators.',
  robots: {
    index: false,
    follow: false,
  },
}
