import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'User Management',
  description: 'Search, filter, and manage platform users on PostPencil.',
  robots: {
    index: false,
    follow: false,
  },
}
